AWSTemplateFormatVersion: 2010-09-09
Transform: 'AWS::Serverless-2016-10-31'

Parameters:
  NotificationTopicARN:
    Type: String
    Default: ''
  NotificationTopicName:
    Type: String
    Default: ''
  TranslateBucketName:
    Type: String
    Default: ''
  GetSignedURLFunctionName:
    Type: String
    Default: ''
  DescriptionBucketName:
    Type: String
    Default: ''

Resources:
  GenerateVideoMetadataFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: index.lambda_handler
      Runtime: python3.9
      MemorySize: 128
      Timeout: 600
      Policies:
        - Statement:
            - Effect: Allow
              Action: 'bedrock:*'
              Resource: '*'

  GenerateVideoDescriptionStateMachine:
    Type: 'AWS::Serverless::StateMachine'
    Properties:
      DefinitionUri: statemachine/state-machine.asl.yml
      DefinitionSubstitutions:
        TranslateBucket: !Ref TranslateBucketName
        DescriptionBucketName: !Ref DescriptionBucketName
        NotificationTopic: !Ref NotificationTopicARN
        GetSignedUrlFunction: !Ref GetSignedURLFunctionName
        GenerateVideoDescriptionFunction: !Ref GenerateVideoMetadataFunction
      Tracing:
        Enabled: true
      Events:
        S3MediaTrigger:
          Type: EventBridgeRule
          Properties:
            EventBusName: default
            Pattern:
              source:
                - aws.s3
              detail-type:
                - Object Created
              detail:
                bucket:
                  name:
                    - !Ref TranslateBucketName
                object:
                  key: [{ prefix: 'validated/' }]
      Policies:
        - S3WritePolicy:
            BucketName: !Ref DescriptionBucketName
        - S3ReadPolicy:
            BucketName: !Ref TranslateBucketName
        - S3WritePolicy:
            BucketName: !Ref TranslateBucketName
        - SNSPublishMessagePolicy:
            TopicName: !Ref NotificationTopicName
        - LambdaInvokePolicy:
            FunctionName: !Ref GenerateVideoMetadataFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref GetSignedURLFunctionName
        - CloudWatchPutMetricPolicy: {}
        - Version: 2012-10-17
          Statement:
            - Sid: XrayAccessPolicy
              Effect: Allow
              Action:
                - 'xray:PutTraceSegments'
                - 'xray:PutTelemetryRecords'
                - 'xray:GetSamplingRules'
                - 'xray:GetSamplingTargets'
                - 'xray:GetSamplingStatisticSummaries'
              Resource: '*'
